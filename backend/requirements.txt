# Phase 1: ADB 控制层
adbutils>=2.0.0
numpy>=1.24.0
opencv-python>=4.8.0

# Phase 2: 识别层
paddleocr>=2.7.0
paddlepaddle>=2.5.0
pyzbar>=0.1.9

# Phase 3-4: 状态机 + 协调器
transitions>=0.9.0

# Phase 5: Web 后端
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0
aiohttp>=3.9.0
python-multipart>=0.0.6

# Phase 6: 桌面窗口
pywebview>=4.4.0

# Phase 7: 打包（仅 Windows）
# nuitka>=2.0  # 在 Windows 上 pip install nuitka
